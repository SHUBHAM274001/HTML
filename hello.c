#include<stdio.h>
    /*char character;
    printf("Enter a character: ");
    scanf(" %c", &character);
    if (character >= '0' && character <= '9') {
        printf("%c is a digit.\n", character);
    } else {
        printf("%c is not a digit.\n", character);
    }

    return 0;
}*/
int main (){
    int i;
    for(i=1;i<=100;i++){
    printf("hello \n");
    }
    return 0;
}


    